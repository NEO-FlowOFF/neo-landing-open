<section id="solution" class="section solution-section">
  <div class="container solution-shell">
    <div class="solution-head">
      <p class="section-label">Arquitetura operacional</p>
      <h2
        class="tech-title"
        aria-label="Não entregamos peças isoladas. Entregamos um sistema de aquisição."
      >
        Não entregamos peças isoladas.
        <span class="cyan-txt">Entregamos um sistema de aquisição.</span
        >
      </h2>
      <p class="section-lead">
        Cada camada precisa cumprir uma função clara: atrair, capturar,
        decidir e reter. Quando isso não existe, o digital vira cenário. Quando
        existe, vira operação.
      </p>
    </div>

    <div class="solution-grid">
      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">01</span>
        <div class="card-badge sale-badge">ATRAÇÃO</div>
        <h3>Camada de Atenção</h3>
        <p>
          <span class="desktop-copy">
            Páginas e narrativas desenhadas para filtrar ruído, enquadrar a
            promessa certa e transformar clique curioso em atenção qualificada.
          </span>
          <span class="mobile-copy">
            Narrativa e interface para transformar curiosidade em atenção
            qualificada.
          </span>
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">02</span>
        <span class="card-badge tech-badge">CAPTURA</span>
        <h3>Camada de Conversão</h3>
        <p>
          <span class="desktop-copy">
            Formulários, checkouts e pontos de entrada que preservam contexto,
            reduzem fricção e impedem que intenção comercial morra no meio do
            caminho.
          </span>
          <span class="mobile-copy">
            Captura com menos fricção e menos abandono entre interesse e ação.
          </span>
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">03</span>
        <span class="card-badge data-badge">DECISÃO</span>
        <h3>Camada de Inteligência</h3>
        <p>
          <span class="desktop-copy">
            Sistemas que organizam sinais, qualificam demanda, roteiam o que
            importa e ajudam sua operação a responder com timing, não com
            improviso.
          </span>
          <span class="mobile-copy">
            Sistemas que transformam sinais em prioridade e decisão comercial.
          </span>
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">04</span>
        <span class="card-badge auto-badge">RETENÇÃO</span>
        <h3>Camada de Continuidade</h3>
        <p>
          <span class="desktop-copy">
            Dashboards, automações e loops de acompanhamento para que cada lead,
            cliente ou recorrência futura continue dentro do ecossistema, e não
            evaporando em planilhas.
          </span>
          <span class="mobile-copy">
            Automação e visibilidade para manter valor circulando no sistema.
          </span>
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .solution-section {
    padding: 10rem 0;
    position: relative;
    content-visibility: auto;
    contain-intrinsic-size: 960px;
  }

  .solution-head {
    max-width: 600px;
    margin-bottom: 4rem;
  }

  .solution-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .solution-grid > * {
    min-width: 0;
  }

  .feature-card {
    position: relative;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 2.5rem;
    transition: all 400ms var(--spring);
    overflow: hidden;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(51, 214, 255, 0.2);
  }

  .feature-tag {
    font-family: var(--font-mono, monospace);
    font-size: 0.75rem;
    color: var(--text-3);
    opacity: 1;
    margin-bottom: 1.5rem;
    display: block;
  }

  .card-badge {
    position: absolute;
    top: 2.5rem;
    right: 2.5rem;
    font-family: var(--font-mono, monospace);
    font-size: 0.55rem;
    font-weight: 900;
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    letter-spacing: 0.1em;
  }

  .sale-badge {
    background: #d9ff3f;
    color: #000;
    box-shadow: 0 0 15px rgba(217, 255, 63, 0.3);
  }
  .tech-badge {
    background: #33d6ff;
    color: #000;
    box-shadow: 0 0 15px rgba(51, 214, 255, 0.3);
  }
  .data-badge {
    background: rgba(255, 255, 255, 0.05);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .auto-badge {
    background: var(--pink);
    color: #fff;
    box-shadow: 0 0 15px rgba(255, 31, 143, 0.3);
  }

  .feature-card h3 {
    font-size: 1.6rem;
    font-weight: 900;
    color: #fff;
    margin-bottom: 1.25rem;
    letter-spacing: -0.01em;
  }

  .feature-card p {
    font-size: 1.05rem;
    color: var(--text-3);
    line-height: 1.6;
    margin: 0;
  }

  .card-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 60px;
    height: 60px;
    background: radial-gradient(
      circle at center,
      rgba(51, 214, 255, 0.1),
      transparent 70%
    );
    pointer-events: none;
    opacity: 0;
    transition: opacity 400ms ease;
  }

  .feature-card:hover .card-glow {
    opacity: 1;
  }

  .mobile-copy {
    display: none;
  }

  @media (min-width: 981px) {
    .solution-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1.5rem;
    }

    .solution-grid .feature-card:nth-child(1),
    .solution-grid .feature-card:nth-child(2),
    .solution-grid .feature-card:nth-child(3),
    .solution-grid .feature-card:nth-child(4) {
      grid-column: auto;
      min-height: unset;
    }

    .solution-grid .feature-card h3 {
      max-width: none;
    }
  }

  @media (max-width: 980px) {
    .solution-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .solution-section {
      padding: 4rem 0;
    }

    .solution-head {
      margin-bottom: 1.5rem;
    }

    .solution-head .section-label {
      margin-bottom: 0.65rem;
    }

    .solution-grid {
      gap: 0.9rem;
    }

    .feature-card {
      padding: 1.15rem;
    }
    .card-badge {
      position: static;
      display: inline-flex;
      margin-bottom: 0.8rem;
    }
    .feature-card h3 {
      font-size: 1.2rem;
      margin-bottom: 0.65rem;
    }
    .feature-card p {
      font-size: 0.94rem;
      line-height: 1.48;
    }
    .desktop-copy {
      display: none;
    }
    .mobile-copy {
      display: inline;
    }
  }
</style>
