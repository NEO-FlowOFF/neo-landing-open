<section id="solution" class="section solution-section">
  <div class="container solution-shell">
    <div class="solution-head">
      <p class="section-label">O que entregamos</p>
      <h2 class="tech-title">Transformamos ativos digitais em <span class="cyan-txt">máquinas de lucro.</span></h2>
      <p class="section-lead">
         Seu site não deve apenas existir. Ele deve operar o seu comercial 24/7 sem falhas.
      </p>
    </div>

    <div class="solution-grid">
      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">01</span>
        <div class="card-badge sale-badge">DIRECT-RESPONSE</div>
        <h3>Landing Pages</h3>
        <p>
          Arquitetura de persuasão desenhada para reduzir fricção, antecipar objeções 
          e transformar curiosidade em intenção real de compra.
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">02</span>
        <span class="card-badge tech-badge">HIGH-ROI</span>
        <h3>E-commerce & Checkouts</h3>
        <p>
          Infraestrutura de varejo escalável com checkouts otimizados de um clique. 
          Foco total em manter o cliente no fluxo até a confirmação do pedido.
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">03</span>
        <span class="card-badge data-badge">SYSTEM-DRIVEN</span>
        <h3>WebApps & Operação</h3>
        <p>
          Sistemas sob medida para gerir fluxos de dados, organizar leads e 
          automatizar tomadas de decisão que antes dependiam de esforço humano.
        </p>
      </article>

      <article class="feature-card cyber-vibe">
        <div class="card-glow"></div>
        <span class="feature-tag">04</span>
        <span class="card-badge auto-badge">AUTOMATED</span>
        <h3>Dashboards & Automação</h3>
        <p>
          Visibilidade em tempo real do seu funil. Conectamos todas as 
          pontas do seu marketing ao seu comercial sem perda de lead.
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .solution-section {
    padding: 10rem 0;
    position: relative;
  }

  .solution-head {
    max-width: 600px;
    margin-bottom: 4rem;
  }

  .solution-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .feature-card {
    position: relative;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 2.5rem;
    transition: all 400ms var(--spring);
    overflow: hidden;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(51, 214, 255, 0.2);
  }

  .feature-tag {
    font-family: var(--font-mono, monospace);
    font-size: 0.75rem;
    color: var(--text-4);
    opacity: 0.5;
    margin-bottom: 1.5rem;
    display: block;
  }

  .card-badge {
    position: absolute;
    top: 2.5rem;
    right: 2.5rem;
    font-family: var(--font-mono, monospace);
    font-size: 0.55rem;
    font-weight: 900;
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    letter-spacing: 0.1em;
  }
  
  .sale-badge { background: #d9ff3f; color: #000; box-shadow: 0 0 15px rgba(217, 255, 63, 0.3); }
  .tech-badge { background: #33d6ff; color: #000; box-shadow: 0 0 15px rgba(51, 214, 255, 0.3); }
  .data-badge { background: rgba(255, 255, 255, 0.05); color: #fff; border: 1px solid rgba(255, 255, 255, 0.1); }
  .auto-badge { background: var(--pink); color: #fff; box-shadow: 0 0 15px rgba(255, 31, 143, 0.3); }

  .feature-card h3 {
    font-size: 1.6rem;
    font-weight: 900;
    color: #fff;
    margin-bottom: 1.25rem;
    letter-spacing: -0.01em;
  }

  .feature-card p {
    font-size: 1.05rem;
    color: var(--text-3);
    line-height: 1.6;
    margin: 0;
  }

  .card-glow {
    position: absolute;
    top: 0; left: 0;
    width: 60px; height: 60px;
    background: radial-gradient(circle at center, rgba(51, 214, 255, 0.1), transparent 70%);
    pointer-events: none;
    opacity: 0;
    transition: opacity 400ms ease;
  }

  .feature-card:hover .card-glow { opacity: 1; }

  @media (max-width: 980px) {
    .solution-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .feature-card { padding: 1.5rem; }
    .card-badge { top: 1.5rem; right: 1.5rem; }
    .feature-card h3 { font-size: 1.4rem; }
  }
</style>
